import React from "react";
import ReactDOM from "react-dom";
import ProfileApp from "./ProfileApp";
import "./index.scss";

const root = document.getElementById("app");
ReactDOM.createRoot(root).render(<ProfileApp />);
